<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        * {
            padding: 0;
            margin: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Lucida Sans', 'Lucida Sans Regular', 'Lucida Grande', 'Lucida Sans Unicode', Geneva, Verdana, sans-serif;
        }

        .main {
            width: 100%;
            height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: space-evenly;
            background: white;
        }

        form {
            width: 500px;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 5px;
            background-color: black;
            border-radius: 12px;
        }
        .data{
            width: 100%;
            display: flex;
            align-items: center;
            justify-content: space-evenly;
            margin: 10px 0px;
        }

        input {
            width: 40%;
            padding: 10px;
            border: none;
            border-radius: 8px;
        }
        input::placeholder{
            text-align: center;
        }

        button {
            padding: 10px 10px;
            font-weight: bold;
            border: none;
            border-radius: 12px;
        }
    </style>
</head>
<body>
    <div class="main">
        <form action="/edit?id=<%=editproduct.id%>" method="post" enctype="multipart/form-data">
            <h3 style="text-decoration: underline orangered 3px; color: white;">Book Edit Data Form</h3>
            <br>
            <div class="data">
                <input type="file" style="background-color: white;" placeholder="Enter Product Image" name="image" value="<%=editproduct.image%>">
                <input type="text" placeholder="Enter Product Name" name="name" value="<%=editproduct.name%>">
            </div>
            <div class="data"> 
                <input type="text" placeholder="Enter Product Category" name="category" value="<%=editproduct.category%>">
                <input type="text" placeholder="Enter Product Price" name="price" value="<%=editproduct.price%>">
            </div>
            <button type="submit">Submit</button>
            <br>
        </form>
    </div>
</body>
</html>